# Frontend environment variables
# This project uses relative /api paths (proxied via nginx or Vercel rewrites).
# No VITE_ variables with secrets should be added here.
# Variables prefixed with VITE_ are exposed to the browser.
